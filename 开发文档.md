# SHDome 开发文档

## 1. 文档信息

- 项目名称：SHDome 多应用部署工具
- 文档版本：0.3
- 当前阶段：应用市场 MVP 已发布（生产版本 `v0.1.6`）
- 目标平台：Linux 服务器
- 默认管理命令：`k`

本文档定义 SHDome 的架构、目录、模块边界、开发规范和测试要求。应用市场 MVP、Docker 生命周期、共享网关、证书、备份和固定版本引导器均已实现，并已通过 GitHub 多发行版/多架构矩阵、真实 Docker 集成和公网 VPS 验收。后续开发应在保持现有命令与状态兼容的前提下扩展独立模块。

## 2. 项目目标

SHDome 需要提供类似 `kejilion.sh` 的一行安装体验：

```bash
bash <(curl -fsSL https://k.flowread.cc)
```

安装完成后不退出，直接进入交互式终端菜单。后续用户可以使用：

```bash
k
k app
k app install zentao
```

一期只实现“应用市场闭环”，需要解决：

1. 在常见 Linux 发行版上完成环境初始化。
2. 以统一方式安装、更新、卸载和查看多个应用。
3. 避免多个应用争抢宿主机端口。
4. 使用一个共享 Nginx 为多个应用提供域名和 HTTPS 入口。
5. 自动申请并安全续期证书。
6. 保持应用数据持久化，并支持备份、恢复和升级回滚。
7. 同时支持交互式终端和参数化命令。

系统更新、清理、BBR、建站、测试脚本、后台工作区和集群管理等能力不进入一期，但项目结构、路由和菜单编号必须为这些后续模块留出稳定扩展空间。

### 2.1 一期范围

- 一行安装和 `k` 交互入口。
- 管理命令在普通 sudoer 环境自动提权；帮助和版本命令保持无特权运行，应用状态与凭据不放宽权限。
- 应用目录、搜索、详情和安装状态。
- 应用安装、更新、启停、日志和卸载。
- Docker 环境初始化。
- 默认 IP+端口访问和端口冲突处理。
- 应用级域名、Nginx、证书、备份等支撑能力。
- SHDome 自更新、状态、锁、日志和回滚基础设施。

### 2.2 后续对标方向

后续参考 `sh` 项目逐步增加：

- 网站与反向代理管理。
- 系统更新、清理和基础工具。
- 网络、内核、BBR、WARP 和防火墙。
- 测试脚本和云平台工具。
- 后台工作区、任务管理和集群控制。
- Web 管理面板和非交互协议。

新增模块不得修改应用市场现有命令语义、状态格式和菜单编号。

## 3. 总体架构

```text
用户终端
  │
  │ bash <(curl -fsSL https://k.flowread.cc)
  ▼
Cloudflare Worker
  │ 只返回小型 bootstrap 引导器
  ▼
GitHub 固定版本/固定 Commit
  │ 下载完整 shdome.sh 和应用目录
  ▼
目标 Linux 服务器
  ├── /usr/local/bin/k
  ├── Docker / Docker Compose
  ├── 共享 Nginx
  ├── Certbot
  ├── 应用容器
  └── 本地状态、数据与备份
```

Cloudflare 只负责安装入口，不存放应用镜像或主要安装制品。主脚本和应用目录由 GitHub 分发，容器镜像由 Docker Hub、GHCR 或应用指定的镜像仓库分发，所有部署动作在用户服务器上完成。

## 4. 安装入口设计

### 4.1 Cloudflare Worker 职责

Worker 只应承担：

- 为安装域名提供 HTTPS。
- 在 `/` 返回 `text/plain` 类型的 bootstrap。
- 将 HTTP 请求重定向到 HTTPS。
- 非根路径返回 404。
- 设置 `Cache-Control: no-store`。
- 发生错误时返回可安全执行并立即退出的 Bash 错误脚本，不返回 HTML 安装内容。

Worker 不承担：

- Docker 部署。
- 应用状态管理。
- 应用镜像存储。
- 目标服务器证书管理。
- 用户数据存储。

### 4.2 Bootstrap 职责

Bootstrap 保持简短，只完成：

1. 检查 root 权限；普通 sudoer 自动通过 `sudo` 重新执行安装器。
2. 检查 `curl`、`sha256sum`、`install` 等基础命令。
3. 识别 CPU 架构和 Linux 环境。
4. 从 GitHub Releases 下载固定版本的完整发布包到临时文件。
5. 校验文件非空、版本对应的严格 Release URL、SHA-256、唯一归档根目录和归档成员类型；拒绝路径穿越、符号链接、硬链接和设备文件。
6. 原子安装到 `/opt/shdome/runtime/<version>`，切换 `current` 软链接，并创建 `$HOME/shdome.sh` 和 `/usr/local/bin/k` 入口。
7. 使用 `exec "$SCRIPT_PATH" "$@"` 透传全部参数。

不得直接覆盖正在执行的主脚本。应使用：

```text
下载到临时文件 → 校验 → chmod → 原子替换 → exec
```

安装根目录、`runtime`、`current` 和已有快捷入口必须验证所有者及真实路径。安装器只允许替换由 root 拥有、最终解析到 `/opt/shdome/runtime/` 内的 SHDome 软链接；普通文件、外部软链接、可被组或其他用户写入的安装目录一律拒绝覆盖。

### 4.3 交互终端保持

必须使用：

```bash
bash <(curl -fsSL https://k.flowread.cc)
```

进程替换会把下载结果作为脚本文件交给 Bash，标准输入仍连接用户终端，因此主脚本可以使用 `read`。主程序应优先从 `/dev/tty` 读取交互输入，避免日志管道或子命令占用标准输入。

Bootstrap 最后一行必须是：

```bash
exec "$SCRIPT_PATH" "$@"
```

不得追加 `</dev/null`。

## 5. 建议仓库结构

```text
shdome/
├── README.md
├── bootstrap/
│   ├── worker.js
│   └── install.sh
├── bin/
│   └── k
├── src/
│   ├── shdome.sh
│   ├── core/
│   │   ├── terminal.sh
│   │   ├── router.sh
│   │   ├── module_registry.sh
│   │   ├── menu_registry.sh
│   │   ├── config.sh
│   │   ├── state.sh
│   │   ├── locks.sh
│   │   ├── logger.sh
│   │   └── self_update.sh
│   └── modules/
│       ├── app_market/
│       │   ├── module.sh
│       │   ├── menu.sh
│       │   ├── catalog.sh
│       │   ├── manifest.sh
│       │   ├── lifecycle.sh
│       │   ├── docker_runtime.sh
│       │   ├── preflight.sh
│       │   ├── port.sh
│       │   ├── gateway.sh
│       │   ├── certificate.sh
│       │   └── backup.sh
│       └── future/
│           └── README.md
├── catalog/
│   ├── zentao.json
│   ├── qinglong.json
│   ├── cloudreve.json
│   ├── uptime-kuma.json
│   └── gitea.json
├── tests/
│   ├── unit/
│   └── smoke/
└── docs/
```

一期使用包含模块源码和应用目录的版本化压缩包发布，`/opt/shdome/current` 原子指向当前版本。核心层不能包含具体应用安装逻辑。主入口只固定加载 `core/`，然后自动发现 `src/modules/*/module.sh`；应用市场通过模块注册接口接入，避免演变成参考项目那样由主菜单和大量业务 `case` 共同组成的单体脚本。

## 6. 目标服务器目录

```text
/opt/shdome/
├── apps/
│   └── <app-id>/
│       ├── compose.yml
│       ├── .env
│       ├── data/
│       ├── logs/
│       └── state.json
├── gateway/
│   ├── nginx.conf
│   ├── conf.d/
│   ├── webroot/
│   └── letsencrypt/
├── catalog/
├── state/
├── backups/
├── locks/
└── logs/

/opt/shdome/runtime/<version>/
/opt/shdome/current -> /opt/shdome/runtime/<version>/

/usr/local/bin/k
$HOME/shdome.sh
```

应用持久化数据必须进入 `/opt/shdome/apps/<app-id>/`。禁止应用清单任意写入 `/etc`、`/root` 或其他未授权路径。

## 7. 模块、菜单与命令分发

### 7.1 模块边界

核心层只负责终端、模块与菜单注册、命令路由、平台设置、状态、锁、日志和更新。应用市场模块拥有 Docker、端口、网关、证书和备份适配器，因为这些能力当前只服务于应用生命周期。未来若其他模块需要复用，再将稳定接口下沉到共享服务层。

每个可发布业务模块必须提供唯一入口：

```text
src/modules/<module_id>/module.sh
```

入口自行加载模块目录内的实现文件，定义注册处理器，并在文件末尾声明：

```bash
module_register app_market app_market_register
```

注册处理器通过 `command_register`、`menu_register` 和 `help_register` 声明命令、菜单及 `k help` 内容。禁止业务模块直接修改 `src/shdome.sh`、核心路由、帮助正文或主菜单源码。`module.sh` 的存在表示该模块可以随正式包加载；未完成的模块只保留设计文档，不创建可加载入口，也不注册占位命令。

模块加载和展示顺序相互独立：模块按发布包目录发现，菜单注册器按数字自动排序。因此后续模块可以独立使用自己的编号段，不依赖源码加载先后顺序。模块间不得直接调用对方内部函数；需要复用的能力先下沉为稳定的核心服务。

### 7.2 命令分发

主程序入口约定：

```text
无参数                         main_menu
app                            app_menu
app list                       app_list
app install <id>               app_install_wizard
app update <id>                app_update
app remove <id>                app_remove_wizard
app status <id>                app_status
app logs <id>                  app_logs
app domain <id>                app_domain
app backup <id>                app_backup
env                            app_environment_menu
config                         settings_menu
version                        show_version
```

分发逻辑示意：

```bash
case "${1:-}" in
    "") main_menu ;;
    app) shift; app_command "$@" ;;
    env) shift; app_environment_command "$@" ;;
    config) shift; config_command "$@" ;;
    version|--version|-v) show_version ;;
    *) show_help; return 2 ;;
esac
```

交互菜单应使用循环和 `return`，避免通过递归反复调用父菜单造成调用栈增长。

### 7.3 主菜单一期形态

```text
SHDome 服务器管理工具
--------------------------------
1. 应用市场
2. 应用运行环境
3. SHDome 设置
00. 更新 SHDome
0. 退出
--------------------------------
```

“已安装应用”不单独注册主菜单项。应用市场负责统一展示序号、名称、版本、状态和说明；交互选择器只接受序号或显示名称，再解析为内部应用 ID，所有条目都进入同一个应用操作菜单。内部 ID 只用于文本命令和脚本兼容。市场页面不再提供安装、搜索和分类子菜单；搜索与分类保留为文本命令。“应用运行环境”包含 Docker 状态、端口、应用域名、证书和备份入口，但这些仍是应用市场的支撑功能，不作为独立产品模块宣传。

### 7.4 菜单编号与未来扩展

菜单编号一旦发布不得重排或复用：

| 编号段 | 模块 | 当前状态 |
|---|---|---|
| `1-9` | 应用市场与运行环境 | 一期实现 |
| `10-19` | 建站、网站和反向代理 | 预留 |
| `20-29` | 系统管理和基础工具 | 预留 |
| `30-39` | 网络、内核和安全 | 预留 |
| `40-49` | 测试和诊断工具 | 预留 |
| `50-59` | 工作区、任务和集群 | 预留 |
| `00` | SHDome 自更新 | 一期实现 |
| `0` | 退出 | 一期实现 |

未实现的编号不显示在主菜单中。后续模块完成后再注册对应菜单项，不在用户界面展示大量“开发中”占位项。命令名称比数字编号更稳定，自动化必须使用 `k app ...` 等文本命令，不能依赖交互菜单数字。

主菜单只承担一级导航，不能把具体应用或每个运维动作全部平铺到首页。当前 `1-4` 是应用市场闭环；未来网站、系统、网络等模块各占一个或少量一级入口，模块内部再展示二级菜单。这保留参考项目的快速入口体验，同时避免功能增加后首页无限增长。

## 8. 终端交互规范

- 启动交互功能前检查 `/dev/tty` 是否可读写。
- 使用 `printf ... >/dev/tty` 输出提示，使用 `read ... </dev/tty` 读取。
- 非 TTY 环境不得永久等待输入。
- SSH 自动化环境需要交互时提示用户使用 `ssh -t`。
- 颜色只在 TTY 中启用。
- `Ctrl+C` 应停止当前操作并执行临时文件清理。
- 安装、卸载、清空数据等重要操作需要确认。
- 同一功能同时保留交互入口和参数入口。
- 所有前置校验必须显式返回失败状态，不能只依赖 `set -e`；交互主菜单使用 `if ! handler` 捕获错误时，Bash 会关闭该处理器调用链中的 `errexit` 语义。

示例：

```bash
k app install zentao
k app install zentao --port 8082 --yes
k app domain zentao --domain zentao.example.com --access domain-only --yes
```

`app domain` 的 `--email` 为可选参数。未提供时使用 Certbot `--register-unsafely-without-email`，交互模式也允许直接留空；提供邮箱时仍会校验格式。

## 9. 应用 Manifest

应用不应通过 `source` 执行远程 Bash 配置。应使用经过 Schema 校验的 YAML 或 JSON：

```yaml
schema: 1
id: zentao
name: 禅道
version: "21.0"
description: 项目管理和研发协作平台

services:
  app:
    image: easysoft/zentao:21.0
    containerName: shdome-zentao
    containerPort: 80

resources:
  diskGB: 2
  memoryMB: 1024

volumes:
  - source: data
    target: /data

backup:
  strategy: cold-filesystem
  logical:
    type: sqlite
    source: app.db
    output: app.sql

healthcheck:
  type: http
  path: /
  port: 80
  timeoutSeconds: 120

routing:
  enabled: true
  defaultAccessMode: direct
  defaultHostPort: 8082
```

必须校验：

- 应用 ID 和容器名格式。
- 镜像地址和版本。
- 容器端口范围。
- 卷目标和宿主机路径范围。
- 环境变量名。
- 备份策略、逻辑备份适配器，以及 SQLite 源是否引用已声明的文件卷。
- 域名格式。
- 健康检查类型和超时。
- CPU 架构兼容性。

生产版本应固定镜像 tag，最好同时记录镜像 digest。

备份配置不是任意命令钩子。当前只允许 `cold-filesystem`，可选的 `sqlite` 适配器使用 Python 标准库执行完整性检查和逻辑导出，不执行 Manifest 提供的 Shell。没有逻辑适配器的应用会在容器停止后执行一致性文件归档。

## 10. 多应用网络与端口

参考 `sh` 项目的应用安装模型，SHDome 默认采用 IP+端口直连。每个应用声明容器内部端口和建议的宿主机端口，安装时必须让用户确认宿主机端口：

```text
服务器:8082 → shdome-zentao:80
服务器:5700 → shdome-qinglong:5700
服务器:5212 → shdome-cloudreve:5212
```

不同容器可以使用相同内部端口，但宿主机端口必须不同：

```text
服务器:8081 → app1:8080
服务器:8082 → app2:8080
服务器:8083 → app3:8080
```

安装成功后默认写入 `accessMode=direct` 和用户确认的 `hostPort`。端口分配必须：

1. 校验为 `1-65535`。
2. 使用 `ss -H -lntu` 检查监听状态。
3. 检查本地状态库。
4. 获取全局端口分配锁。
5. 让 Docker 执行最终绑定校验。
6. 成功后写入状态。

更新或重建容器时复用已登记的宿主机端口。卸载时释放端口登记。包含多个端口映射的应用必须在 Manifest 中列出全部宿主机端口，并逐一检查，不能只检查主端口。

### 10.1 `sh` 项目参考行为

SHDome 的默认访问流程以 `sh` 项目中的 `docker_app` 和 `docker_app_plus` 为基线：

1. 应用定义默认 `docker_port`。
2. 用户选择安装后，安装器提示确认或修改端口。
3. 使用 `ss` 检查端口占用。
4. 通过 `docker run -p <hostPort>:<containerPort>` 或 Compose 发布端口。
5. 将端口写入应用状态，参考实现使用 `<container>_port.conf`。
6. 将访问模式登记为 `direct`。
7. 安装结束立即显示 IPv4/IPv6 的访问地址。
8. 用户之后选择“添加域名访问”，才调用 Nginx、证书和端口访问策略。

SHDome 在保留上述用户体验的基础上，增加全部端口校验、并发锁、健康检查、原子状态和失败回滚。

## 11. 共享 Nginx

Nginx 不是安装普通应用的前置条件。只有用户为已安装应用添加域名时，系统才检查、安装或复用共享 Nginx。只有一个网关容器占用宿主机 `80/443`，每个应用生成独立配置：

```text
/opt/shdome/gateway/conf.d/zentao.example.com.conf
/opt/shdome/gateway/conf.d/qinglong.example.com.conf
```

为了与 `sh` 项目保持一致，Nginx 使用宿主机网络，并代理到应用已经发布的宿主机端口：

```text
zentao.example.com   → 127.0.0.1:8082
qinglong.example.com → 127.0.0.1:5700
```

交互菜单添加域名完成后默认切换为 `domain_only`。该模式通过 Compose 将宿主机端口绑定到回环地址，阻止公网直接访问应用端口，同时使 Nginx 仍能连接 `127.0.0.1:<hostPort>`。选择“允许 IP+端口访问”时恢复为 `direct`，选择“阻止 IP+端口访问”时重新切换为 `domain_only`；删除域名也会恢复为 `direct`。

部署共享 Nginx 前必须检查 `80/443` 占用。不得为了安装 Nginx 自动停止或强制删除未知容器、进程；发生冲突时应显示占用者并让用户处理。

修改配置的事务流程：

```text
生成临时配置
→ nginx -t
→ 原子替换正式配置
→ nginx -s reload
→ 失败恢复旧配置
```

同一域名默认只绑定一个应用。同域名按路径路由属于后续高级功能。

## 12. 证书管理

域名接入前必须检查：

- A/AAAA 是否解析到当前服务器。
- 80/443 是否可访问。
- 是否存在冲突配置。
- 系统时间是否准确。

首次签发优先使用 Certbot Webroot HTTP-01，避免停止 Nginx。证书续期使用 systemd timer 或独立定时任务，每天检查，但只在进入续期窗口时执行。

正确续期流程：

```text
保留旧证书
→ 申请/续期新证书
→ 验证证书域名、有效期和私钥
→ 原子替换
→ nginx -t
→ reload
→ 失败继续使用旧证书
```

禁止先删除旧证书再申请新证书。

## 13. 应用生命周期事务

### 13.1 安装

```text
获取应用锁
→ 校验 Manifest
→ 检查资源和依赖
→ 提示并校验宿主机端口
→ 创建临时目录与密码
→ 生成 Compose
→ 拉取镜像
→ 启动容器
→ 健康检查
→ 写入端口与 direct 访问状态
→ 显示 IPv4/IPv6 的 IP+端口地址
→ 释放锁
```

域名、Nginx 和证书属于安装完成后的可选流程，不应阻塞默认 IP+端口安装。

### 13.2 更新

```text
锁定应用
→ 短暂停止应用并创建配置、状态和持久化数据的一致性备份
→ 记录旧镜像 digest
→ 拉取新镜像
→ 重建容器
→ 健康检查
→ 成功提交新状态
→ 失败或中断时回滚旧镜像、配置和持久化数据
```

备份先在隐藏暂存目录中生成归档、SHA-256、元数据和可选 Nginx 配置，全部成功后才以 `.tar.gz` 为最后一步发布。失败或中断不得在备份列表留下半成品，并必须恢复备份前正在运行的应用。

### 13.3 卸载

默认卸载仅删除运行资源并保留数据。彻底删除必须显式使用 `--purge`，并要求二次确认：

```bash
k app remove zentao
k app remove zentao --purge
```

## 14. 状态、锁和幂等性

状态至少记录：

- 应用 ID、版本和镜像 digest。
- 容器名和容器 ID。
- 域名、内部端口和直连端口。
- 访问模式。
- 安装时间和最后更新时间。
- Compose 和 Manifest 摘要。
- 数据目录和备份信息。

状态文件采用临时文件加原子替换。需要以下锁：

- 全局初始化锁。
- Docker 安装锁。
- 应用目录刷新锁。
- 单应用生命周期锁。
- 端口分配锁。
- Nginx 配置锁。
- 证书签发锁。

重复运行 bootstrap、`k setup` 或安装命令不得覆盖已有数据。

## 15. 安全要求

- 主脚本使用固定 tag/commit，并进行 SHA-256 校验。
- 应用目录不能直接 `source` 远程代码。
- 镜像固定版本并记录 digest。
- 密码使用安全随机数生成，敏感文件权限为 `0600`。
- 禁止默认弱密码。
- 禁止默认 `privileged` 容器。
- 严格限制卷挂载路径。
- 所有用户输入必须防止 Shell 注入和路径穿越。
- 卸载数据必须可确认、可备份。
- 日志不得输出密码、Token 和私钥。
- 自动更新必须能够回滚，不直接跟随可变 `main`。

## 16. 测试要求

最低测试矩阵：

- Ubuntu、Debian、Rocky/AlmaLinux、Alpine。
- amd64 和 arm64。
- 有/无 Docker 的新机器。
- 有/无 IPv6。
- SSH TTY、`ssh -t` 和非 TTY。
- 三个应用使用相同容器内部端口。
- 三个应用分别绑定 HTTPS 域名。
- 服务器重启后恢复。
- 证书续期和 Nginx reload。
- 并发安装争抢同一端口。
- 更新失败回滚。
- 卸载一个应用不影响其他应用。
- GitHub 下载失败、下载到 HTML、校验失败。

## 17. 实施顺序

### 17.1 应用市场 MVP（已完成）

1. Bootstrap、Worker 和主脚本安装。
2. 核心路由、菜单注册、TTY 和 `k` 命令。
3. 应用市场主菜单、目录、Manifest 和状态展示。
4. Docker 初始化、Compose 生成和单应用生命周期。
5. 默认 IP+端口、冲突检查、状态和并发锁。
6. 应用更新、日志、卸载、健康检查和失败回滚。

### 17.2 应用市场增强（已完成当前范围）

1. 应用级 Nginx 域名反代。
2. 证书签发和安全自动续期。
3. 应用备份、恢复和迁移。
4. 应用目录签名、自动化测试和发布流程。
5. 非交互协议和 Web 面板适配。

### 17.3 后续模块

按照预留编号段依次实现建站、系统、网络、安全、测试、工作区和集群模块。每个模块必须通过统一注册接口接入，不得在主入口追加不可维护的超大分支。
