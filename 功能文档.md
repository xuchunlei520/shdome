# SHDome 功能文档

## 1. 产品定位

SHDome 是面向 Linux 服务器的模块化管理工具。`v0.1.8` 已形成应用市场闭环：用户通过一条命令安装 SHDome，安装结束后直接进入交互式终端，并通过统一的 `k` 命令管理官方应用和自定义应用。

项目后续会参考 `sh` 项目增加建站、系统、网络、安全、测试和集群等能力，但这些功能不进入应用市场 MVP。Docker、端口、域名、证书和备份在一期作为应用市场的内部支撑能力提供。

安装入口：

```bash
bash <(curl -fsSL https://k.flowread.cc)
```

快捷入口：

```bash
k
k app
k app install zentao
```

当前仓库已经包含模块化命令入口、应用目录、Docker 生命周期、端口管理、可选域名/证书、备份和发布引导器，并已完成 GitHub 托管 CI、多发行版矩阵及公网 VPS 端到端验收。

## 2. 用户角色

### 2.1 服务器管理员

- 初始化服务器环境。
- 安装、更新和卸载应用。
- 配置域名和 HTTPS。
- 查看状态、日志和资源。
- 执行备份与恢复。

### 2.2 应用维护者

- 编写应用 Manifest。
- 指定镜像、端口、卷、环境变量和健康检查。
- 测试应用生命周期。
- 提交应用目录更新。

### 2.3 自动化调用方

- 通过参数执行无人值守安装。
- 获取机器可读的状态和退出码。
- 与 Web 面板、API 或运维流水线集成。

## 3. 核心功能总览

| 模块 | 核心能力 | 优先级 | 当前状态 |
|---|---|---|---|
| 一行安装 | Cloudflare 引导器、GitHub Release、校验和原子安装 | P0 | 已发布 |
| 权限入口 | 普通 sudoer 执行管理命令时自动提权，敏感状态仍仅 root 可读 | P0 | 已发布 |
| 交互终端 | 安装后直接进菜单、`k` 主菜单、TTY 输入 | P0 | 已发布 |
| 系统初始化 | 权限、系统、资源、网络和依赖检查 | P0 | 已发布 |
| 应用运行环境 | 安装 Docker/Compose、容器和端口状态 | P0 | 已发布 |
| 应用市场 | 列表、状态、说明、序号/名称选择；搜索和分类 CLI | P0 | 已发布 |
| 应用生命周期 | 安装、更新、卸载、启动、停止、日志 | P0 | 已发布 |
| 多应用端口 | 默认 IP+端口访问、冲突检查和端口状态登记 | P0 | 已发布 |
| 状态管理 | 状态记录、对账、锁和幂等性 | P0 | 已发布 |
| 更新回滚 | 应用版本升级、健康检查和失败回滚 | P0 | 已发布 |
| 域名反代 | 应用级共享 Nginx、一应用一域名 | P1 | 已发布 |
| HTTPS | 应用证书申请、查看、续期和失败恢复 | P1 | 已发布 |
| 备份恢复 | 应用配置、数据和数据库备份恢复 | P1 | 已发布 |
| 自动化接口 | `--yes`、结构化输出、稳定退出码 | P2 | 已发布基础接口 |

优先级定义：

- P0：应用市场 MVP，第一阶段必须交付。
- P1：应用市场增强；当前域名、证书和备份能力已经随 MVP 发布。
- P2：自动化和面板集成；当前已提供 CLI 自动化接口，Web 面板属于后续阶段。
- Future：对标 `sh` 的其他服务器管理模块，不阻塞应用市场发布。

## 4. 一行安装

### 4.1 目标行为

用户执行：

```bash
bash <(curl -fsSL https://k.flowread.cc)
```

系统依次完成：

1. Cloudflare 返回小型 bootstrap。
2. Bootstrap 检查权限和必要命令；普通 sudoer 自动提权。
3. 从 GitHub Releases 下载固定版本发布包。
4. 校验归档路径和 SHA-256。
5. 原子安装版本目录并创建 `$HOME/shdome.sh` 兼容入口。
6. 安装 `/usr/local/bin/k`。
7. 直接显示交互式主菜单。

安装器还会严格匹配版本、Release URL 和发布包文件名，校验归档唯一根目录，并拒绝归档中的路径穿越、链接及设备文件。已有安装目录、版本链接和 `k` 快捷入口只有在所有者及真实目标属于 SHDome 时才允许原子替换。

### 4.2 参数透传

```bash
bash <(curl -fsSL https://k.flowread.cc) app
```

应直接进入应用市场。

```bash
bash <(curl -fsSL https://k.flowread.cc) app install zentao
```

应直接进入禅道安装向导。

## 5. 主菜单

目标菜单：

```text
SHDome 服务器管理工具
--------------------------------
1. 应用市场
2. 应用运行环境
3. SHDome 设置
00. 更新 SHDome
0. 退出
--------------------------------
请输入选择:
```

无效输入不退出程序，提示后重新显示菜单。一期不显示尚未实现的未来模块。

主菜单是平台级导航，不直接列出具体应用和细分操作。“已安装应用”合并到“应用市场”：市场表格使用序号、名称、版本、状态、来源和说明列。未安装应用输入序号或名称后直接进入一次确认安装，已安装应用进入统一操作菜单；输入 `A` 添加自定义应用。

菜单扩展规则：

- `1-9` 固定用于应用市场和运行环境。
- `10-19` 预留网站与建站。
- `20-29` 预留系统管理。
- `30-39` 预留网络、内核和安全。
- `40-49` 预留测试和诊断。
- `50-59` 预留工作区、任务和集群。
- 已发布编号永不改号或复用。
- 自动化使用 `k app ...` 文本命令，不依赖菜单数字。
- 模块注册顺序不决定展示顺序，菜单统一按数字排序。
- 未完成模块不提供可加载入口，不显示占位菜单。

## 6. 系统初始化

首次运行需要检查：

- 是否为 root。
- 是否为支持的 Linux 发行版。
- CPU 架构是否为 amd64/arm64。
- 磁盘、内存和 Swap 是否满足要求。
- 系统时间是否正确。
- GitHub 和镜像仓库是否可访问。
- Docker 和 Compose 是否已安装。
- 防火墙和安全组提示。

初始化操作必须幂等，重复执行不得覆盖应用数据。

默认 IP+端口安装不要求 `80/443` 空闲。只有用户添加应用域名时才检查 Nginx 所需的 `80/443`。

## 7. 应用市场

应用市场支持：

- 显示全部应用。
- 合并官方目录和不会被平台升级覆盖的用户自定义目录，并显示来源。
- 通过序号或应用名称直接选择应用。
- 搜索和分类能力保留为 `k app search/category/categories` 命令，不占用交互菜单。
- 显示应用描述、版本、资源需求和架构支持。
- 显示未安装、运行中、已停止、异常和可更新状态。
- 未安装应用自动选择可用端口，只需一次确认；已安装应用统一进入更新、卸载、域名和访问模式操作页。
- 支持从固定版本 Docker 镜像生成受限的单容器自定义应用。
- 支持使用可信公钥验签、原子刷新和回滚官方目录。
- Docker Hub 缓慢或不可达时自动选择健康镜像源，失败时逐源回退并复用固定版本本地缓存；普通安装不增加确认步骤。

示例：

```text
应用市场
--------------------------------
1. 禅道              未安装
2. 青龙              运行中
3. Cloudreve         可更新
4. Uptime Kuma       已停止
0. 返回
```

## 8. 应用安装向导

`k app install zentao` 应进入向导：

1. 显示应用信息和资源需求。
2. 检查 Docker、磁盘和架构。
3. 显示应用建议的宿主机端口。
4. 自动选择可用宿主机端口；显式传入 `--port` 时严格使用指定值。
5. 生成安全随机密码。
6. 显示最终计划。
7. 用户确认后执行容器部署。
8. 等待健康检查。
9. 将访问模式登记为 `direct`。
10. 显示 IPv4/IPv6 的 `IP:端口` 地址和后续命令。
11. 提示用户可在统一应用操作菜单中继续添加域名访问。

安装成功标准：

- 容器处于运行状态。
- 健康检查成功。
- 数据目录存在且权限正确。
- 宿主机端口与容器端口映射正确。
- 如用户后续添加域名，Nginx 配置和 HTTPS 证书通过校验。
- 状态记录完整写入。

## 9. 多应用端口模型

### 9.1 默认直连模式

应用安装时默认映射宿主机端口：

```text
服务器:8081 → zentao:80
服务器:8082 → app2:80
```

平台负责端口校验、锁定、登记和释放，Docker 负责最终绑定检查。安装完成后立即显示：

```text
http://服务器IPv4:8081
http://[服务器IPv6]:8081
```

不同容器内部可以使用相同端口，但宿主机端口不能重复。应用包含多个端口映射时，平台必须检查全部映射。

### 9.2 可选域名模式

域名访问不是普通应用安装的前置条件。用户安装完成后选择“添加域名访问”，系统才部署或复用 Nginx，并将域名转发到应用宿主机端口：

```text
zentao.example.com → 127.0.0.1:8081
```

成功后访问模式可以切换为 `domain_only`，阻止公网直接访问 `8081`，但保留 Nginx 对本机端口的访问。用户也可以恢复 `direct` 模式。

## 10. 应用管理

单应用操作菜单：

```text
应用：禅道
状态：未安装
域名访问：未配置
IP+端口访问：不可用
--------------------------------
1. 安装              2. 更新            3. 卸载
--------------------------------
5. 添加域名访问      6. 删除域名访问
7. 允许IP+端口访问   8. 阻止IP+端口访问
--------------------------------
0. 返回上一级选单
```

交互菜单聚焦高频部署流程。添加域名时默认切换为 `domain_only`；删除域名会恢复 `direct`。状态、日志、启停、凭据、证书详情、备份和恢复等完整能力继续由下面的文本命令提供，便于自动化且不挤占交互菜单。

命令入口：

```bash
k app status zentao
k app logs zentao
k app start zentao
k app stop zentao
k app restart zentao
k app update zentao
k app remove zentao
```

## 11. 域名反向代理

平台按需复用一个共享 Nginx。未配置域名的应用不要求安装 Nginx：

```text
zentao.example.com   → 127.0.0.1:8081
qinglong.example.com → 127.0.0.1:5700
```

每个域名独立配置，所有域名共享宿主机 `80/443`。新增配置必须先执行 `nginx -t`，校验失败不得影响现有配置。

功能包括：

- 添加域名。
- 删除域名。
- 更换域名。
- HTTP 跳转 HTTPS。
- WebSocket 代理。
- 上传大小和超时设置。
- 查看域名对应应用。
- 域名冲突检测。

## 12. HTTPS证书

功能包括：

- 自动申请 Let's Encrypt 证书。
- 查看证书颁发者和到期时间。
- 每日检查续期窗口。
- Webroot 无中断续期。
- 续期成功后校验并 reload Nginx。
- 续期失败保留旧证书。
- 记录失败日志并提示管理员。
- 支持导入已有证书。

证书申请前提示用户确认 DNS 已解析到当前服务器。

## 13. Docker管理

- 查看容器、镜像和网络。
- 启动、停止和重启容器。
- 查看日志和资源占用。
- 查看容器端口和网络地址。
- 安装或修复 Docker 环境。
- 清理未使用镜像，但不得默认删除应用数据卷。
- 显示 SHDome 管理的容器和外部容器区别。
- 自动探测 Docker Hub 和可信 HTTPS 镜像源，并显示当前模式、最近成功来源和失败摘要。
- 自动选源只影响 SHDome 拉取任务，不覆盖 `daemon.json` 或重启 Docker。

## 14. 更新与回滚

主程序更新：

- 查询固定发布版本。
- 下载到临时文件。
- 校验 SHA-256。
- 备份当前脚本。
- 原子替换。
- 失败自动恢复。

应用更新：

- 显示当前和目标版本。
- 更新前短暂停止应用并创建配置、状态和持久化数据的一致性备份。
- 保留旧镜像 digest。
- 更新后执行健康检查。
- 失败或中断时从一致性备份回滚旧容器、配置和持久化数据。

## 15. 备份与恢复

备份范围：

- 应用 Manifest 和 Compose。
- `.env` 和非明文密钥引用。
- 应用持久化目录。
- 声明了受支持适配器的数据库逻辑备份；当前支持 SQLite，Cloudreve 会额外包含 `cloudreve.sql`。
- Nginx 域名配置。
- 证书元数据。
- SHDome 状态。

备份先在隐藏暂存目录中生成归档、SHA-256、元数据和可选 Nginx 配置，全部成功后才发布 `.tar.gz`。失败或中断的备份不进入备份列表，并恢复备份前正在运行的应用。

未声明逻辑适配器的单容器应用采用停机一致性文件归档。应用清单不能提供任意备份 Shell，只能选择平台白名单中的备份类型。

支持：

```bash
k app backup zentao
k app restore zentao <backup-id>
k backup all
k restore <backup-id>
```

## 16. 交互和自动化模式

交互模式：

```bash
k
k app
k app install zentao
```

自动化模式：

```bash
k app install zentao \
  --port 8082 \
  --access direct \
  --yes
```

自动安装完成后添加域名：

```bash
k app domain zentao \
  --domain zentao.example.com \
  --access domain-only \
  --yes
```

`--email` 为可选参数。省略时 Certbot 使用无邮箱注册模式；提供邮箱时可接收到期和续期异常通知。

自动化模式要求：

- 不调用 `read`。
- 参数不足立即失败。
- 返回稳定退出码。
- 可选 JSON 输出。
- 不输出 ANSI 颜色。

## 17. 安全功能

- 固定主脚本版本并校验。
- 固定容器镜像版本。
- 应用 Manifest Schema 校验。
- 不直接执行远程应用 Shell 配置。
- 安全随机密码。
- 敏感文件权限限制。
- 危险操作确认。
- 应用安装锁和端口锁。
- 操作审计日志。
- 防止命令注入、路径穿越和任意卷挂载。

## 18. MVP验收标准

1. 一行命令完成安装并直接进入菜单。
2. 安装后 `k` 和 `k app` 可用。
3. 至少支持三个示例应用。
4. 三个应用可以使用相同容器内部端口和不同宿主机端口。
5. 默认安装完成后可立即通过 IP+端口访问。
6. 三个应用可以在安装后分别绑定 HTTPS 域名。
7. 服务器重启后应用和已配置的 Nginx 自动恢复。
8. 端口冲突能够在安装前发现。
9. 健康检查失败不登记为安装成功。
10. 卸载一个应用不影响其他应用。
11. 更新失败能够恢复旧版本。

## 19. 后续功能路线

应用市场 MVP 完成并稳定后，再按模块增加：

| 编号段 | 后续模块 | 参考能力 |
|---|---|---|
| `10-19` | 网站与建站 | LDNMP、静态站点、反向代理、负载均衡 |
| `20-29` | 系统管理 | 更新、清理、软件包、磁盘、账户和 SSH |
| `30-39` | 网络与安全 | BBR、WARP、防火墙、Fail2ban、DNS |
| `40-49` | 测试与诊断 | 网络、硬件、流媒体和综合测试 |
| `50-59` | 工作区与集群 | 后台任务、远程节点和集群控制 |

后续模块共用现有核心路由、菜单注册、状态、锁、日志和更新机制，但不允许直接依赖应用市场内部实现。需要复用的稳定能力应先抽象成共享服务接口。
