# SHDome 使用文档

## 1. 文档说明

本文档描述 SHDome 应用市场 MVP 计划提供的用户操作方式。当前 `shdome` 目录处于设计阶段，尚未包含可执行安装器。建站、系统优化、网络测试和集群等对标 `sh` 的扩展功能将在应用市场稳定后按模块增加。

## 2. 使用前准备

目标服务器应满足：

- Linux 服务器。
- root 用户或等效管理员权限。
- amd64 或 arm64 CPU。
- 可以访问 GitHub 和容器镜像仓库。
- 建议至少 2 GB 内存和 10 GB 可用磁盘。
- 如需域名访问，公网安全组开放 TCP 80/443。
- 域名 A/AAAA 记录解析到服务器。

不建议直接在已有重要业务的生产服务器上首次测试。请先备份网站、数据库、容器和系统配置。

## 3. 一行安装

终端中执行：

```bash
bash <(curl -fsSL https://mytool.sh)
```

注意：终端命令必须使用普通 URL，不能粘贴 Markdown 链接形式：

```text
[https://mytool.sh](https://mytool.sh)
```

安装流程：

1. 检查 root 权限。
2. 从 GitHub 下载 SHDome 主脚本。
3. 校验下载内容。
4. 保存主脚本。
5. 安装 `k` 快捷命令。
6. 直接进入交互式主菜单。

预期界面：

```text
SHDome 应用部署与管理工具
--------------------------------
1. 应用市场
2. 已安装应用
3. 应用运行环境
4. SHDome 设置
00. 更新 SHDome
0. 退出
--------------------------------
请输入选择:
```

一期只显示已经实现的应用市场功能，不显示大量“开发中”菜单项。未来功能会使用预留的新编号加入，现有菜单编号和 `k app ...` 命令不会改变。

## 4. 打开主菜单

安装后执行：

```bash
k
```

如果提示找不到命令：

```bash
hash -r
```

或者重新登录 SSH 后再执行 `k`。

## 5. 打开应用市场

从主菜单选择“应用市场”，或者直接执行：

```bash
k app
```

查看应用列表：

```bash
k app list
```

预期输出：

```text
应用ID         应用名称          状态
zentao         禅道              未安装
qinglong       青龙              未安装
cloudreve      Cloudreve         未安装
uptime-kuma    Uptime Kuma       未安装
```

## 6. 安装应用

### 6.1 从菜单安装

执行：

```bash
k app
```

选择应用后选择“安装”。安装向导会询问：

- 应用对外服务端口。
- 是否使用应用建议的默认端口。
- 数据目录和资源确认。
- 最终执行确认。

默认安装完成后通过 `IP:端口` 访问。域名是应用安装完成后的可选配置，不是默认安装的必填项。

### 6.2 直接进入安装向导

```bash
k app install zentao
```

示例过程：

```text
应用：禅道
输入应用对外服务端口，回车默认使用8082端口:

安装计划
--------------------------------
应用：       禅道
容器：       shdome-zentao
访问模式：   IP+端口
宿主机端口： 8082
内部端口：   80
数据目录：   /opt/shdome/apps/zentao
--------------------------------
确认开始安装吗？[y/N]: y

安装完成：
http://服务器IP:8082
```

### 6.3 一行安装平台并进入指定应用

```bash
bash <(curl -fsSL https://mytool.sh) app install zentao
```

引导器安装 SHDome 后，会直接进入禅道安装向导。

## 7. 应用访问方式

### 7.1 IP+端口访问（默认）

```text
http://服务器IP:8082
```

安装时每个应用选择一个未占用的宿主机端口：

```text
服务器:8082 → shdome-zentao:80
```

多个容器可以使用相同内部端口，但宿主机端口必须不同：

```text
服务器:8081 → app1:8080
服务器:8082 → app2:8080
```

如果端口已被占用，安装向导会要求重新输入。

### 7.2 域名访问（可选）

应用已经通过 IP+端口运行后，可以继续添加域名：

```text
https://zentao.example.com
```

SHDome 此时才安装或复用共享 Nginx，并将域名转发到应用宿主机端口：

```text
zentao.example.com → 127.0.0.1:8082
```

用户可以继续保留公网 IP+端口访问，或者切换为仅域名访问并阻止公网直接连接 `8082`。

## 8. 配置域名

配置前完成 DNS 解析：

```text
zentao.example.com A    服务器IPv4
```

只有服务器确实提供 IPv6 服务时才添加 AAAA。

进入指定应用的域名设置：

```bash
k app domain zentao
```

系统会检查：

- 域名格式。
- DNS 是否指向本机。
- 80/443 是否可访问。
- 域名是否已被其他应用使用。
- Nginx 配置是否有效。

## 9. HTTPS证书

应用默认安装不申请证书。绑定域名后，SHDome 才自动申请 Let's Encrypt 证书。

申请成功后访问：

```text
https://zentao.example.com
```

查看证书状态：

```bash
k app certs
```

手动续期：

```bash
k app cert renew zentao.example.com
```

自动续期任务会定期检查证书剩余时间。续期失败时继续使用旧证书，并记录错误日志。

常见签发失败原因：

- DNS 尚未生效。
- A 记录指向错误服务器。
- 存在错误的 AAAA 记录。
- 云安全组未开放 80/443。
- 服务器防火墙拦截请求。
- 达到 Let's Encrypt 签发频率限制。

## 10. 查看应用状态

查看全部已安装应用：

```bash
k app installed
```

查看单个应用：

```bash
k app status zentao
```

预期信息包括：

- 应用版本。
- 容器状态和健康状态。
- 镜像版本。
- 域名。
- 访问模式。
- 直连端口。
- 数据目录。
- 最后更新时间。

## 11. 日志和常用操作

查看日志：

```bash
k app logs zentao
```

持续查看日志：

```bash
k app logs zentao --follow
```

启动、停止和重启：

```bash
k app start zentao
k app stop zentao
k app restart zentao
```

## 12. 更新应用

```bash
k app update zentao
```

更新前系统应显示：

- 当前版本。
- 目标版本。
- 预计停机影响。
- 备份状态。

更新后会执行健康检查。健康检查失败时自动恢复旧镜像和配置。

## 13. 备份与恢复

备份单个应用：

```bash
k app backup zentao
```

查看备份：

```bash
k app backups zentao
```

恢复：

```bash
k app restore zentao <backup-id>
```

备份全部应用：

```bash
k backup all
```

数据库应用会使用数据库逻辑备份，不应只压缩正在运行的数据目录。

## 14. 卸载应用

卸载运行环境并保留数据：

```bash
k app remove zentao
```

彻底删除应用及数据：

```bash
k app remove zentao --purge
```

`--purge` 会删除应用数据，必须二次确认。建议先执行：

```bash
k app backup zentao
```

## 15. 更新SHDome

交互菜单选择：

```text
00. 更新 SHDome
```

或者执行：

```bash
k self-update
```

更新流程会下载固定发布版本、校验、备份旧脚本并原子替换。失败时恢复旧版本。

## 16. 自动化安装

默认 IP+端口无人值守示例：

```bash
k app install zentao \
  --port 8082 \
  --access direct \
  --yes
```

安装完成后添加域名：

```bash
k app domain zentao \
  --domain zentao.example.com \
  --access domain-only \
  --yes
```

自动化模式下参数不足会直接失败，不会等待终端输入。

## 17. SSH终端问题

如果提示“当前操作需要交互终端”，请使用：

```bash
ssh -t root@服务器IP
```

然后执行：

```bash
k
```

不要使用下面的管道方式启动交互安装：

```bash
curl -fsSL https://mytool.sh | bash
```

推荐使用：

```bash
bash <(curl -fsSL https://mytool.sh)
```

## 18. 常见问题

### 18.1 `k: command not found`

执行：

```bash
hash -r
```

检查：

```bash
ls -l /usr/local/bin/k /usr/bin/k
```

### 18.2 GitHub下载失败

检查：

```bash
curl -I https://raw.githubusercontent.com
```

可以在引导器中配置可信的 GitHub 代理，但不应静默执行未经校验的代理内容。

### 18.3 Docker镜像拉取失败

检查：

```bash
docker info
docker pull hello-world
```

确认 DNS、代理和镜像加速配置。

### 18.4 端口已被占用

查看监听端口：

```bash
ss -lntu
```

查看 Docker 映射：

```bash
docker ps --format 'table {{.Names}}\t{{.Ports}}'
```

### 18.5 域名可以解析但无法访问

检查：

```bash
curl -I http://域名
curl -I https://域名
```

同时检查安全组、服务器防火墙、Nginx 状态和容器健康状态。

## 19. 安全提醒

- 只从官方安装域名执行引导器。
- 正式环境优先使用固定版本，不跟随可变 `main`。
- 安装前备份现有业务。
- 不要把 Token、密码和私钥粘贴到公开日志。
- 不要在不了解后果时使用 `--purge`。
- 定期验证备份能否恢复。

## 20. 后续功能说明

应用市场完成后，SHDome 会继续参考 `sh` 项目增加网站、系统、网络、安全、测试、工作区和集群模块。扩展功能会使用独立命令，例如：

```text
k web       网站与建站（规划）
k system    系统管理（规划）
k network   网络与安全（规划）
k test      测试与诊断（规划）
k cluster   集群控制（规划）
```

上述规划命令在功能完成前不会显示在主菜单，也不应返回虚假的成功结果。
