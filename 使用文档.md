# SHDome 使用文档

## 1. 文档说明

本文档描述 SHDome 应用市场 MVP 的用户操作方式。当前生产版本为 `v0.1.6`，公开仓库已发布对应 GitHub Release，并通过 Cloudflare Worker 在 `k.flowread.cc` 提供生产引导器。建站、系统优化、网络测试和集群等对标 `sh` 的扩展功能将在应用市场稳定后按模块增加。

## 2. 使用前准备

目标服务器应满足：

- Linux 服务器。
- root 用户或等效管理员权限。
- amd64 或 arm64 CPU。
- 可以访问 GitHub 和容器镜像仓库。
- 建议至少 2 GB 内存和 10 GB 可用磁盘。
- 如需域名访问，公网安全组开放 TCP 80/443。
- 域名 A/AAAA 记录解析到服务器。

不建议直接在已有重要业务的生产服务器上首次测试。请先备份网站、数据库、容器和系统配置。

## 3. 一行安装

终端中执行：

```bash
bash <(curl -fsSL https://k.flowread.cc)
```

注意：终端命令必须使用普通 URL，不能粘贴 Markdown 链接形式：

```text
[https://k.flowread.cc](https://k.flowread.cc)
```

安装流程：

1. Worker 返回固定版本、固定 SHA-256 和固定 Commit 的小型引导器。
2. 检查 root 权限；普通 sudoer 会自动通过 `sudo` 获取安装权限。
3. 从固定 Commit 下载安装器，从 GitHub Release 下载版本归档。
4. 校验 Release URL、文件名、SHA-256 和安全归档结构。
5. 原子安装到 `/opt/shdome/versions/<版本>` 并切换 `current` 链接。
6. 安装 `/usr/local/bin/k` 和兼容入口。
7. 直接进入交互式主菜单。

预期界面：

```text
SHDome 服务器管理工具
--------------------------------
1. 应用市场
2. 应用运行环境
3. SHDome 设置
00. 更新 SHDome
0. 退出
--------------------------------
请输入选择:
```

一期只显示已经实现的应用市场功能，不显示大量“开发中”菜单项。“已安装应用”不重复占用主菜单：应用市场中的状态列会显示安装状态，选择任意应用后直接进入统一操作菜单。未来功能会以独立模块和预留的新编号加入；具体应用始终在应用市场二级菜单中展示，不会挤满主菜单。

## 4. 打开主菜单

安装后执行：

```bash
k
```

SHDome 的应用状态和生成凭据保存在 root 专用目录中。使用普通 sudoer 账号执行 `k` 或 `k app ...` 时，程序会自动调用 `sudo` 并在需要时请求当前账号的 sudo 密码，不需要手工先执行 `sudo -i`。`k version` 和 `k help` 不会请求提权。如果账号没有 sudo 权限，请先切换到 root。

如果提示找不到命令：

```bash
hash -r
```

或者重新登录 SSH 后再执行 `k`。

## 5. 打开应用市场

从主菜单选择“应用市场”，或者直接执行：

```bash
k app
```

查看应用列表：

```bash
k app list
```

按分类浏览或输出机器可读 JSON：

```bash
k app categories
k app category 开发工具
k app list --json
```

预期输出：

```text
序号   名称                 版本           状态       说明
1      Cloudreve            3.8.3          未安装     支持多种存储策略的自托管网盘系统
2      Gitea                1.22.6         未安装     轻量级自托管 Git 服务，示范 Web 与 SSH 多端口部署
3      青龙面板             2.17.12        未安装     支持定时任务管理的自动化面板
4      Uptime Kuma          1.23.16        运行中     轻量易用的服务可用性监控面板
5      禅道                 21.7           未安装     项目管理与研发协作平台
```

应用市场不再提供“安装应用、搜索应用、按分类浏览”三个交互子菜单。直接输入序号或应用名称，例如 `4`、`Uptime Kuma`、`禅道`，即可进入对应应用的统一操作菜单。`uptime-kuma` 这类内部应用 ID 不在交互界面展示或接受，只用于 `k app ...` 命令和兼容旧脚本。

统一操作菜单：

```text
1. 安装              2. 更新            3. 卸载
--------------------------------
5. 添加域名访问      6. 删除域名访问
7. 允许IP+端口访问   8. 阻止IP+端口访问
--------------------------------
0. 返回上一级选单
```

添加域名访问成功后默认阻止公网直接访问应用端口，只保留 Nginx 通过回环地址访问应用。需要同时保留 IP+端口入口时，选择 `7`；需要恢复仅域名访问时，选择 `8`。状态、日志、启停、凭据、备份和恢复等高级操作继续通过 `k app ...` 命令提供。

## 6. 安装应用

### 6.1 从菜单安装

执行：

```bash
k app
```

选择应用后选择“安装”。安装向导会询问：

- 应用对外服务端口。
- 是否使用应用建议的默认端口。
- 数据目录和资源确认。
- 最终执行确认。

默认安装完成后通过 `IP:端口` 访问。域名是应用安装完成后的可选配置，不是默认安装的必填项。

### 6.2 直接进入安装向导

```bash
k app install zentao
```

示例过程：

```text
应用：禅道
输入应用对外服务端口，回车默认使用8082端口:

安装计划
--------------------------------
应用：       禅道
容器：       shdome-zentao
访问模式：   IP+端口
宿主机端口： 8082
内部端口：   80
数据目录：   /opt/shdome/apps/zentao
--------------------------------
确认开始安装吗？[y/N]: y

安装完成：
http://服务器IP:8082
```

### 6.3 一行安装平台并进入指定应用

```bash
bash <(curl -fsSL https://k.flowread.cc) app install zentao
```

引导器安装 SHDome 后，会直接进入禅道安装向导。

## 7. 应用访问方式

### 7.1 IP+端口访问（默认）

```text
http://服务器IP:8082
```

安装时每个应用选择一个未占用的宿主机端口：

```text
服务器:8082 → shdome-zentao:80
```

多个容器可以使用相同内部端口，但宿主机端口必须不同：

```text
服务器:8081 → app1:8080
服务器:8082 → app2:8080
```

如果端口已被占用，安装向导会要求重新输入。

多端口应用可以重复使用 `--port`。数字形式修改主端口，`名称=端口` 修改指定端口：

```bash
k app install gitea --port http=3000 --port ssh=2222 --yes
```

### 7.2 域名访问（可选）

应用已经通过 IP+端口运行后，可以继续添加域名：

```text
https://zentao.example.com
```

SHDome 此时才安装或复用共享 Nginx，并将域名转发到应用宿主机端口：

```text
zentao.example.com → 127.0.0.1:8082
```

从统一应用操作菜单选择“添加域名访问”时，系统默认切换为仅域名访问并阻止公网直接连接 `8082`。之后可选择“允许IP+端口访问”恢复 `direct`，或选择“阻止IP+端口访问”恢复 `domain_only`。使用 CLI 配置时也可以通过 `--access direct|domain-only` 明确指定。

## 8. 配置域名

配置前完成 DNS 解析：

```text
zentao.example.com A    服务器IPv4
```

只有服务器确实提供 IPv6 服务时才添加 AAAA。

通过 CLI 进入指定应用的域名设置：

```bash
k app domain zentao
```

交互模式也可以从应用市场选择对应序号或名称，再选择 `5. 添加域名访问`；选择 `6. 删除域名访问` 会移除 Nginx 配置并恢复 IP+端口访问。

系统会检查：

- 域名格式。
- DNS 是否指向本机。
- 80/443 是否可访问。
- 域名是否已被其他应用使用。
- Nginx 配置是否有效。

## 9. HTTPS证书

应用默认安装不申请证书。绑定域名后，SHDome 才自动申请 Let's Encrypt 证书。通知邮箱是可选项：填写邮箱可接收到期和续期异常通知，留空时使用 Certbot 的无邮箱注册模式。

申请成功后访问：

```text
https://zentao.example.com
```

查看证书状态：

```bash
k app certs
```

手动续期：

```bash
k app cert renew zentao.example.com
```

自动续期任务会定期检查证书剩余时间。续期失败时继续使用旧证书，并记录错误日志。

导入已有证书：

```bash
k app cert import zentao \
  --domain zentao.example.com \
  --cert /path/fullchain.pem \
  --key /path/privkey.pem \
  --yes
```

常见签发失败原因：

- DNS 尚未生效。
- A 记录指向错误服务器。
- 存在错误的 AAAA 记录。
- 云安全组未开放 80/443。
- 服务器防火墙拦截请求。
- 达到 Let's Encrypt 签发频率限制。

## 10. 查看应用状态

查看全部已安装应用：

```bash
k app installed
```

查看单个应用：

```bash
k app status zentao
```

机器可读状态：

```bash
k app status zentao --json
```

预期信息包括：

- 应用版本。
- 容器状态和健康状态。
- 镜像版本。
- 域名。
- 访问模式。
- 直连端口。
- 数据目录。
- 最后更新时间。

## 11. 日志和常用操作

查看日志：

```bash
k app logs zentao
```

持续查看日志：

```bash
k app logs zentao --follow
```

启动、停止和重启：

```bash
k app start zentao
k app stop zentao
k app restart zentao
```

## 12. 更新应用

```bash
k app update zentao
```

更新前系统应显示：

- 当前版本。
- 目标版本。
- 预计停机影响。
- 备份状态。

更新前会短暂停止应用并创建一致性备份。更新后执行健康检查；拉取、启动、健康检查失败或操作被中断时，会从该备份恢复旧镜像、配置和持久化数据。

## 13. 备份与恢复

备份单个应用：

```bash
k app backup zentao
```

查看备份：

```bash
k app backups zentao
```

恢复：

```bash
k app restore zentao <backup-id>
```

备份全部应用：

```bash
k backup all
```

SHDome 先停止单容器应用再归档持久化数据。声明了白名单适配器的数据库还会生成逻辑备份；当前 Cloudreve 的 SQLite 数据库会额外生成 `cloudreve.sql` 并放入归档，应用清单不能执行任意备份 Shell。

备份文件只有在归档、SHA-256、元数据和可选 Nginx 配置全部生成成功后才会出现在列表中；中断的备份不会留下可误选的半成品。

## 14. 卸载应用

卸载运行环境并保留数据：

```bash
k app remove zentao
```

彻底删除应用及数据：

```bash
k app remove zentao --purge
```

`--purge` 会删除应用数据，必须二次确认。建议先执行：

```bash
k app backup zentao
```

## 15. 更新SHDome

交互菜单选择：

```text
00. 更新 SHDome
```

或者执行：

```bash
k self-update
```

更新流程会下载固定发布版本、校验、备份旧脚本并原子替换。失败时恢复旧版本。

## 16. 自动化安装

默认 IP+端口无人值守示例：

```bash
k app install zentao \
  --port 8082 \
  --access direct \
  --yes
```

安装完成后添加域名：

```bash
k app domain zentao \
  --domain zentao.example.com \
  --access domain-only \
  --yes
```

如需接收证书通知，可额外添加 `--email admin@example.com`。自动化模式下域名等必需参数不足会直接失败，不会等待终端输入。

## 17. SSH终端问题

如果提示“当前操作需要交互终端”，请使用：

```bash
ssh -t root@服务器IP
```

然后执行：

```bash
k
```

不要使用下面的管道方式启动交互安装：

```bash
curl -fsSL https://k.flowread.cc | bash
```

推荐使用：

```bash
bash <(curl -fsSL https://k.flowread.cc)
```

## 18. 常见问题

### 18.1 `k: command not found`

执行：

```bash
hash -r
```

检查：

```bash
ls -l /usr/local/bin/k /usr/bin/k
```

### 18.2 GitHub下载失败

检查：

```bash
curl -I https://raw.githubusercontent.com
```

可以在引导器中配置可信的 GitHub 代理，但不应静默执行未经校验的代理内容。

### 18.3 Docker镜像拉取失败

检查：

```bash
docker info
docker pull hello-world
```

确认 DNS、代理和镜像加速配置。

### 18.4 端口已被占用

查看监听端口：

```bash
ss -lntu
```

查看 Docker 映射：

```bash
docker ps --format 'table {{.Names}}\t{{.Ports}}'
```

### 18.5 域名可以解析但无法访问

检查：

```bash
curl -I http://域名
curl -I https://域名
```

同时检查安全组、服务器防火墙、Nginx 状态和容器健康状态。

## 19. 安全提醒

- 只从官方安装域名执行引导器。
- 正式环境优先使用固定版本，不跟随可变 `main`。
- 安装前备份现有业务。
- 不要把 Token、密码和私钥粘贴到公开日志。
- 不要在不了解后果时使用 `--purge`。
- 定期验证备份能否恢复。

## 20. 后续功能说明

应用市场完成后，SHDome 会继续参考 `sh` 项目增加网站、系统、网络、安全、测试、工作区和集群模块。扩展功能会使用独立命令，例如：

```text
k web       网站与建站（规划）
k system    系统管理（规划）
k network   网络与安全（规划）
k test      测试与诊断（规划）
k cluster   集群控制（规划）
```

上述规划命令在功能完成前不会显示在主菜单，也不应返回虚假的成功结果。
